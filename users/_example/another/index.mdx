---
title: Another one
template: default
unlisted: true
---

An example to demonstrate using the basic template instead of the default one.

The default template doesn't do any layout. The basic template does a few
things that might be commonly needed:

-   Putting the contents in a column

-   Adding page footer links
